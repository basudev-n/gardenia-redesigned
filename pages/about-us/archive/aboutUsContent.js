// Archived structured content for About Us (moved from frontend/src/content)
// Kept here as a backup before removing live content.

const aboutUsContent = {
  // archived content omitted for brevity
};

export default aboutUsContent;
